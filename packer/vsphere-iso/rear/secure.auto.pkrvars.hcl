
# vCenter password for user defined in
# "vsphere_user" variable (no defaults):
vsphere_password = ""

# ssh password for user defined in
# "ssh_username" variable (no defaults):
# example(!):
ssh_password     = "R00t_P@ssw0rd_01!"
